---
import BackToTop from "../components/BackToTop.astro";
import PromoModal from "../components/PromoModal.astro";
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>UI Demo · Ascension</title>

    <!-- Bootstrap 5 for dropdown navbar (Argon-like behavior) -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      --panel:#0f172a;     /* card/nav background */
        --ink:#e5e7eb;       /* primary text */
        --muted:#94a3b8;     /* secondary text */
        --accent:#3b82f6;    /* link / accent */
      }
      html, body { height:100%; }
      body{
        background:var(--bg);
        color:var(--ink);
        /* Quote multi-word family to avoid parser confusion */
        font-family: Inter, system-ui, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
        margin:0;
      }
      .navbar{
        background:var(--panel)!important;
        border-bottom:1px solid rgba(148,163,184,.18);
      }
      .navbar-brand, .nav-link{ color:var(--ink)!important; }
      .nav-link:hover{ color:#ffffff!important; }

      /* Avoid CSS functions that older parsers sometimes mis-handle */
      .container-narrow{
        width:92vw;
        max-width:1100px;
        margin-left:auto;
        margin-right:auto;
        padding:2rem 0;
      }
      .card{
        background:var(--panel);
        color:var(--ink);
        border:1px solid rgba(148,163,184,.18);
      }
      a{ color:var(--accent); }
    </style>
  </head>
  <body>
    <!-- Dropdown Navbar -->
    <nav class="navbar navbar-expand-lg">
      <div class="container-fluid" style="max-width:1100px">
        /Ascension</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navDemo" aria-controls="navDemo" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navDemo">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">/notes/Notes</a></li>
            <li class="nav-item dropdown">
              #Teachings</a>
              <ul class="dropdown-menu">
                <li>#Foundations</a></li>
                <li>#Hylozoics</a></li>
                <li><hr class="dropdown-divider"></li>
                <li>#All series</a></li>
              </ul>
            </li>
          </ul>
          <span class="navbar-text" style="color:#cbd5e1">UI Demo</span>
        </div>
      </div>
    </nav>

    <main class="container-narrow">
      <div class="card p-4 mb-4">
        <h1 class="h4 mb-2">UI Demo</h1>
        <p class="mb-0">Dropdown navbar, timed modal popup, and floating “Back to top”. Dark slate background (no tan).</p>
      </div>

      <div class="card p-4">
        <h2 class="h5">Some long content</h2>
        <p>Scroll down to see the “Back to top” button appear. A modal will also display a few seconds after load. This page is additive and does not change your homepage.</p>
        <div style="height:1200px"></div>
        <p>Bottom of demo content.</p>
      </div>
    </main>

    <!-- Timed popup & Back to top -->
    <PromoModal client:idle delayMs={6000} />
    <BackToTop client:idle />

    <!-- Bootstrap JS bundle for dropdowns -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
     >
